/*
  # Security Headers for BIM Shamans
  # Ochrona przed najczęstszymi atakami web (XSS, Clickjacking, MITM)
  
  # Zapobieganie clickjacking - strona nie może być pokazana w iframe na innych stronach
  X-Frame-Options: DENY
  
  # Zapobieganie MIME type sniffing - przeglądarka musi respektować podane typy plików
  X-Content-Type-Options: nosniff
  
  # Kontrola informacji o źródle - chroni prywatność użytkowników
  Referrer-Policy: strict-origin-when-cross-origin
  
  # Blokada dostępu do wrażliwych funkcji urządzenia (geolokalizacja, kamera, mikrofon)
  Permissions-Policy: geolocation=(), microphone=(), camera=(), payment=(), usb=(), magnetometer=(), gyroscope=(), accelerometer=()
  
  # Wymuszenie HTTPS - przeglądarka będzie używać tylko szyfrowanego połączenia przez rok
  Strict-Transport-Security: max-age=31536000; includeSubDomains; preload

